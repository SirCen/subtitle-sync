// Home route. Server component that renders the real, browser-only
// subtitle-sync workspace (a client component).
import SubtitleSync from "./SubtitleSync";

export default function Home() {
  return <SubtitleSync />;
}
